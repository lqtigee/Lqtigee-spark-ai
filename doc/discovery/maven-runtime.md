# Maven Runtime Discovery

Ticket: `P0-M005`

Command:

```bash
mvn -version
```

Observed:

```text
Apache Maven 3.8.7
Maven home: /usr/share/maven
Java version: 21.0.11, vendor: Ubuntu, runtime: /usr/lib/jvm/java-21-openjdk-arm64
OS name: "linux", version: "6.17.0-1021-nvidia", arch: "aarch64", family: "unix"
```

Conclusion:

```text
Maven is available and uses Java 21.
```

