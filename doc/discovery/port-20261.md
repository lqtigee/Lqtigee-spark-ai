# Port 20261 Discovery

Ticket: `P0-M003`

Command:

```bash
ss -ltnp
```

Observation:

- No listener was found on port `20261`.
- Existing Java listeners were found on ports `19011` and `19012`.
- Existing local AI/service ports include `11434`, `11435`, `11436`, `11437`, `11438`, `21435`, `21436`, and `21442`.

Conclusion:

```text
Port 20261 is available at discovery time.
```

Risk note:

- This is a point-in-time observation. The backend startup ticket must still fail clearly if port `20261` is occupied later.

